@{
    ViewData["Title"] = "Home Page";
}

@model List<Bowler>

<h1>Bowlers</h1>

<ul>
    @foreach (var b in Model)
    {
        <li>@b.BowlerFirstName @b.BowlerMiddleInit @b.BowlerLastName |
            @b.BowlerAddress @b.BowlerCity, @b.BowlerState @b.BowlerZip |
            @b.BowlerPhoneNumber
        </li>
    }
</ul>